# BCOD-Sim anonymous offline release

Run `npm ci`, `npm test`, and `npm run test:release`. This reproduces software evidence only; see `manifest.json` for external validation gates. SIMMAN-derived raw files are cache-only and explicitly excluded.
