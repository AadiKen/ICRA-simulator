import assert from "node:assert/strict";import {readFile} from "node:fs/promises";const a=JSON.parse(await readFile("artifacts/gazebo/vehicle-c/open-loop-trajectory-comparison-thrust-corrected.json","utf8"));assert.equal(a.provenance.controller,"none; fixed-input open-loop");assert.deepEqual(a.scenarios.map((x:any)=>x.id),["straight-ahead","pure-lateral","rotation-in-place","allocation-chirp"]);for(const s of a.scenarios){assert.equal(s.bcod_sim.length,120);assert.equal(s.gazebo_harmonic.length,120);assert.equal(s.error_time_series.length,120);assert.equal(s.command_schedule.length,120);assert.deepEqual(s.bcod_sim.map((x:any)=>x.command),s.gazebo_harmonic.map((x:any)=>x.command));assert.ok(s.gazebo_harmonic.every((x:any,i:number)=>Number.isFinite(x.delivered.port_azimuth_rad)&&Number.isFinite(x.delivered.starboard_azimuth_rad)&&Math.abs(x.time_s-(i+1)*.05)<1e-9&&Number.isInteger(x.gazebo_iteration)));assert.equal(s.synchronization.blind_retries,false);assert.equal(s.synchronization.asserted_iteration_delta,5);}console.log("Vehicle C open-loop paired artifact passed");
