import {createHash} from "node:crypto";import {mkdir,readFile,writeFile} from "node:fs/promises";import {gazeboOdomToTask} from "../rl-campaign/ports/task-trace-bridge.ts";
const ROOT="artifacts/gazebo/vehicle-c",IDS=["straight-ahead","pure-lateral","rotation-in-place","allocation-chirp"],sha=async(p:string)=>createHash("sha256").update(await readFile(p)).digest("hex"),pct=(a:number[],p:number)=>[...a].sort((x,y)=>x-y)[Math.min(a.length-1,Math.ceil(p*a.length)-1)],stats=(a:number[])=>({mean:a.reduce((s,x)=>s+x,0)/a.length,p95:pct(a,.95),max:Math.max(...a),final:a.at(-1)}),wrap=(x:number)=>Math.abs(Math.atan2(Math.sin(x),Math.cos(x)));
const scenarios=[];for(const id of IDS){const node=JSON.parse(await readFile(`${ROOT}/node-reference/${id}.json`,"utf8")),gz=JSON.parse(await readFile(`${ROOT}/open-loop-corrected/${id}.json`,"utf8")),schedule=JSON.parse(await readFile(`${ROOT}/schedules/${id}.json`,"utf8"));if(node.rows.length!==gz.rows.length||node.rows.length!==schedule.samples.length)throw Error(`${id}: unmatched row counts`);if(gz.rows.some((r:any,i:number)=>r.iteration_delta!==5||Math.abs(r.sim_time_delta_s-.05)>1e-12||Math.abs(r.time_s-(i+1)*.05)>1e-9))throw Error(`${id}: Gazebo synchronization invariant failed`);const bcod=node.rows.map((r:any,i:number)=>({time_s:(i+1)*.05,north_m:r.state.N_m,east_m:r.state.E_m,heading_rad:r.state.yaw_rad,command:schedule.samples[i],delivered:{port_thrust_n:r.applied.port.thrust_n,port_azimuth_rad:r.applied.port.azimuth_rad,starboard_thrust_n:r.applied.starboard.thrust_n,starboard_azimuth_rad:r.applied.starboard.azimuth_rad}})),gazebo=gz.rows.map((r:any)=>{const s=gazeboOdomToTask(r.time_s,r.enu);return{time_s:r.time_s,gazebo_iteration:r.gazebo_iteration,gazebo_sim_time_s:r.gazebo_sim_time_s,north_m:s.N_m,east_m:s.E_m,heading_rad:s.yaw_rad,command:r.command,delivered:r.delivered}}),errors=bcod.map((r:any,i:number)=>({time_s:r.time_s,horizontal_position_error_m:Math.hypot(r.north_m-gazebo[i].north_m,r.east_m-gazebo[i].east_m),heading_error_rad:wrap(r.heading_rad-gazebo[i].heading_rad)})),position=errors.map((x:any)=>x.horizontal_position_error_m),heading=errors.map((x:any)=>x.heading_error_rad),increments=position.map((x:number,i:number)=>i?x-position[i-1]:0),jumpIndex=increments.indexOf(Math.max(...increments)),jump=increments[jumpIndex],medianIncrement=pct(increments.map(Math.abs),.5);scenarios.push({id,dt_s:.05,synchronization:gz.synchronization,command_schedule:schedule.samples,bcod_sim:bcod,gazebo_harmonic:gazebo,error_time_series:errors,summary:{horizontal_position_error_m:stats(position),heading_error_rad:stats(heading),divergence_pattern:jump>Math.max(.05,10*medianIncrement)?{kind:"sharp-jump",time_s:errors[jumpIndex].time_s,increment_m:jump}:{kind:"steady-growth",largest_increment_time_s:errors[jumpIndex].time_s,largest_increment_m:jump}}});}
const artifact={schema_version:1,artifact_kind:"vehicle-c-open-loop-fixed-input-trajectory-comparison",timing:{physics_timestep_s:.05,full_resolution:true},scenarios,provenance:{generator:{path:"validation/gazebo-vehicles/generate_vehicle_c_reference.ts",sha256:await sha("validation/gazebo-vehicles/generate_vehicle_c_reference.ts")},runner:{path:"validation/gazebo-vehicles/run_gazebo_vehicle_c_open_loop.py",sha256:await sha("validation/gazebo-vehicles/run_gazebo_vehicle_c_open_loop.py")},sdf:{path:"gazebo/models/vehicle-c-azimuth/model.sdf",sha256:await sha("gazebo/models/vehicle-c-azimuth/model.sdf"),thrust_cap_correction:{task:"vehicle-c-gazebo-thrust-fix",date:"2026-09-07",forward_n:1340.506075016061,reverse_n:670.2530375080305}},parameter_map:{path:"validation/gazebo-vehicles/vehicle-c-parameter-map.json",sha256:await sha("validation/gazebo-vehicles/vehicle-c-parameter-map.json")},controller:"none; fixed-input open-loop"}};await mkdir(ROOT,{recursive:true});await writeFile(`${ROOT}/open-loop-trajectory-comparison-thrust-corrected.json`,JSON.stringify(artifact,null,2)+"\n");console.log(JSON.stringify(Object.fromEntries(scenarios.map((x:any)=>[x.id,x.summary])),null,2));
