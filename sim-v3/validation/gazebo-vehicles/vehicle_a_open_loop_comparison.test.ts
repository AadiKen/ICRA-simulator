import assert from "node:assert/strict";import {readFile} from "node:fs/promises";const a=JSON.parse(await readFile("artifacts/gazebo/vehicle-a/open-loop-trajectory-comparison.json","utf8"));assert.equal(a.vehicle,"vehicle-a-otter");assert.equal(a.scenarios.length,6);for(const s of a.scenarios){assert.equal(s.bcod_sim.length,s.gazebo_harmonic.length);assert.equal(s.bcod_sim.length,s.error_time_series.length);assert.equal(s.bcod_sim.length,s.actuation.requested_schedule.length);assert.ok(s.bcod_sim.every((x:any,i:number)=>JSON.stringify(x.command)===JSON.stringify(s.gazebo_harmonic[i].command)));}console.log("Vehicle A open-loop paired artifact passed");
