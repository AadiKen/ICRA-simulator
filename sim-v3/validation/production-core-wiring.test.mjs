import assert from "node:assert/strict";import{readFileSync}from"node:fs";for(const file of["schema.js","core/dynamicsCore.js","core/coupledSixPlant.js","gazebo/captureUtils.js"]){const text=readFileSync(new URL(`../${file}`,import.meta.url),"utf8");assert.doesNotMatch(text,/core\/forces\/(actuatorModel|addedMassCoriolis|hydrodynamicDamping|hydrostaticsAndWaves|windLoad|currentCoupling|waveExcitation)/,`${file} must use the typed core directly.`);}console.log("Production physics is wired directly to the typed core.");
