from pathlib import Path


def build(data_paths=None) -> Path:
    raise NotImplementedError(
        "Figure 1 is blocked until the Vehicle A 1.5M phase-1 result is known and "
        "Vehicle B/C classical reference-success artifacts exist. PPO training is intentionally not started."
    )
