import{resolveExperiment}from"../../packages/experiment-schema/src/index.ts";import{HeadlessMarineSimulation}from"../../packages/core/src/simulation.ts";import{LegacyProductionEngine}from"../../backends/node/src/legacy-production-engine.ts";
const plant=process.argv[2] as"planar3"|"coupled6",steps=Number(process.argv[3]??20),dt=Number(process.argv[4]??.05),vehicle=process.argv[5]??(plant==="planar3"?"vehicle-a-otter":"vehicle-c-azimuth"),config=resolveExperiment({schema_version:1,experiment:{name:`direct-${plant}`,seed:73,timestep_s:dt,duration_s:steps*dt+1},backend:{type:"node"},vehicle:{preset:vehicle,plant},mission:{type:"hold"}} as any),sim=new HeadlessMarineSimulation(new LegacyProductionEngine()),trace=[];sim.reset(config);for(let step=0;step<steps;step++){const phase=step/steps,action=plant==="planar3"?{portCommand:.2+.3*phase,starboardCommand:.4-.2*phase}:{surgeForce:20+10*phase,yawMoment:2-4*phase};sim.step(action);trace.push(sim.getGroundTruth());}sim.dispose();console.log(JSON.stringify({plant,vehicle,steps,dt,trace}));
