import assert from "node:assert/strict";
import {ActuationModel} from "../../../packages/core/src/actuators.js";
const thruster=(id:string,y:number):any=>({id,type:"FixedThruster",pos:[0,y,0],axis:[1,0,0],maxForwardThrust:80,maxReverseThrust:40,dynamics:{tau:0.2,rateMax:200},conversion:{type:"linear",deadZone:0.05},power:{idleW:2,controlW:5,maxW:120}});
const model:any=new ActuationModel({geometry:{beam:1.2},massProps:{cg:{x:0,y:0,z:0}},actuator:{behaviorVersion:"integrated-v1",maxThrust:80,beam:1.2},effectors:[thruster("port",-0.6),thruster("starboard",0.6)]});model.commandWrench({effectors:{port:{command:0.8},starboard:{command:0.8}}},0.1);const wrench=model.commandWrench({failureStates:{port:{mode:"failed-off",source:"usv-bench-36/resources-actuator-failure"}},effectors:{port:{command:0.8},starboard:{command:0.8}}},0.1);const events=model.drainEvents();const summary={scenario_id:"resources-actuator-failure",failure_category:events.some((event:any)=>event.type==="ACTUATOR_FAILURE")?"ACTUATOR_FAILURE":null,event_count:events.length,finite_wrench:wrench.every(Number.isFinite)};assert.deepEqual(summary,{scenario_id:"resources-actuator-failure",failure_category:"ACTUATOR_FAILURE",event_count:1,finite_wrench:true});assert.equal(model.effectors[0].thrust,0);console.log("USV-Bench-36 integrated actuator-failure smoke passed.");
