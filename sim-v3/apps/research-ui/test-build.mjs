import assert from "node:assert/strict";import {readdir,readFile} from "node:fs/promises";const files=await readdir(new URL("./dist/assets/",import.meta.url));const js=files.find((name)=>name.endsWith(".js"));assert.ok(js);const source=await readFile(new URL(`./dist/assets/${js}`,import.meta.url),"utf8");for(const phrase of ["Evidence boundary","field-data-unavailable","No Cummins radiation memory","USV-Bench-36"])assert.ok(source.includes(phrase),phrase);console.log("Research UI build tests passed.");
